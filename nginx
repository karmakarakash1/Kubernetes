sudo su

cd /etc/nginx

ls

cd sites-available/

ls

nano nginx.conf

upstream python {

	server localhost:8000;
}


server {
	listen 80;
	server_name _;

	location / {
		proxy_pass http://python;
	}
}



nano /etc/hosts

sudo ln -s /etc/nginx/sites-available/nginx.conf /etc/nginx/sites-enabled

cd ../sites-enabled/

rm -rf default 

cd ..

sudo nginx -t

cd sites-available/

sudo nano

sudo nginx -t

sudo service nginx restart

exit

ls

sudo su

cd /etc/nginx

ls

cd sites-enabled/

ls

sudo service nginx restart

exit



ls

cd workdir

ls
 
mkdir nginx

cd nginx

nano Dockerfile 

FROM nginx
RUN rm /etc/nginx/conf.d/default.conf
COPY nginx.conf /etc/nginx/conf.d/nginx.conf


docker build -t nginx:v1 .

docker run -it --net=host nginx:v1

sudo service nginx stop

docker run -it --net=host nginx:v1

cat /etc/nginx/sites-available/nginx.conf 

 


ALTER USER 'akash123'@'localhost' IDENTIFIED BY 'new_password';







